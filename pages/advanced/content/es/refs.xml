<root>
  <title>`innerRef`</title>
  <subtitle>Cómo accede al subyacente `ref`</subtitle>
  <description>
    <![CDATA[
      A veces usted necesita el acceso a la ['ref'] (https://facebook.github.io/react/docs/refs-and-the-dom.html) del componente subyacente que se representa. Puede lograr esto con el prop `innerRef`.

      Esta es una función y se llamará siempre que, con el elemento interno.

      ```interactive {clickToRender: true, summary: 'An input in a form'}
      const MyInput = glamorous.input({ borderRadius: 4, padding: '4px 8px' });

      class MyComponent extends React.Component {
        constructor() {
          this.alertValue = e => {
            e.preventDefault();
            alert(this._inputRef.value);
          };
        }
        render() {
          return (
            
          );
        }
      }

      render(<MyComponent />)
      ```
    ]]>
  </description>
</root>